# deploy/homelab

Docker Compose deployment for `companion-core`, `reachy-hub`, PostgreSQL, and
(later) a vector store. Kubernetes is explicitly out of scope for initial
releases (see docs section 1, Non-goals).

Not yet implemented — scaffolding only (Phase 0). Implementation starts in Phase 4.
